import { createChatBotMessage } from "react-chatbot-kit";

const config = {
  botName: "TaskBot 💖",
  initialMessages: [
    createChatBotMessage("Hi 👋 I’m your AI assistant! Ask me anything 💕"),
  ],
};

export default config;